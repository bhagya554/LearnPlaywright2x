# Chapter 10: Loops

Repeating code with `for`, `while`, and `do...while` loops, plus loop control with `break` / `continue`.

## Files

| File | Topic |
|------|-------|
| `71_For_Loop.js` | Basic `for` loop counting 0–9 |
| `72_For_Loop.js` | `for` loop with `<` vs `<=` conditions |
| `73_For_Loop2.js` | Loop counters as identifiers (valid variable names) |
| `74_IQ.js` | Infinite loops, conditional logic inside a loop |
| `75_For_Of_In_Each.js` | Placeholder for `for...of`, `for...in`, and `forEach` |
| `76_While_loop.js` | `while` loop basics |
| `77_Do_While_Loop.js` | `do...while` runs at least once (retry flaky test idea) |
| `78_Do_While.js` | `do...while` retry counter example |
| `79_IQ.js` | Counting down with a `while` loop |
| `80_IQ.js` | `do...while` that runs once even when the condition is false |
| `81_IQ.js` | Skipping an iteration with `continue` |
| `82_IQ.js` | Comparing `while` vs `do...while`; spotting an infinite loop |
