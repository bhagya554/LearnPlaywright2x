# Chapter 18: Async / Await

Using `async`/`await` syntax to work with Promises — writing cleaner asynchronous code, handling errors with `try`/`catch`/`finally`, running tasks in parallel vs sequentially, and real Playwright test examples.

## Files

| File | Topic |
|------|-------|
| `161_async_await.js` | `async`/`await` basics — `try`/`catch`/`finally` with rejected promises |
| `162_async_p2.js` | Awaiting resolved promises, `.then()` vs `await`, rejected promise behaviour |
| `163_PyramidOfDoomResolved.js` | Resolving callback hell using `async`/`await` with Promise-based browser steps |
| `164_Async_Ex.js` | `async` functions returning values, using `await`, Playwright test example |
| `165_AA_Paralle.js` | Running multiple API calls in parallel with `Promise.all` + `await` |
| `165_AA_Sequential.js` | Sequential `await` calls with timing measurement |
| `166_IQ.js` | Interview questions — async function return values, `Promise.resolve`, multi-step flows |
| `167_AC_Login.js` | Playwright test using `async`/`await` for a login flow |
