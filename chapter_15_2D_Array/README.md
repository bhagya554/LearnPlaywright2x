# Chapter 15: 2D Arrays

Creating, accessing, iterating over, and transforming two-dimensional arrays, plus star pattern printing.

## Files

| File | Topic |
|------|-------|
| `138_2D_Array.js` | 2D array basics — grid creation, nested iteration using `grid[i].length` |
| `139_IQ.js` | Interview questions — iterating over a 2D matrix with nested loops |
| `140_REAL.js` | Real-world example — test result matrix (test name, status, response time) |
| `141_2D_Array_Fn.js` | Functional approach — `map` + `reduce` for row sums; `includes()` for fail detection; nested loop patterns |
| `142_Right_star_pattern.js` | Right-aligned star pattern using nested loops |
| `143_Reverse_Star_pattern.js` | Reverse star pattern (descending rows) using nested loops |
