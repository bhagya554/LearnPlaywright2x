# Chapter 13: Strings

String creation, properties, searching, substrings, transforming, replacing, concatenation, splitting, joining, and conversion.

## Files

| File | Topic |
|------|-------|
| `118_Strings.js` | String creation — single quotes, double quotes, template literals, multiline, `String()` conversion |
| `119_String_Properties.js` | `length` property, accessing by index `[]`, `at()`, `charAt()`, `charCodeAt()` |
| `120_Search_Check_String.js` | Searching — `includes`, `startsWith`, `endsWith`, `indexOf`, `lastIndexOf` |
| `121_Substring.js` | Substring extraction — `slice`, `substring`, `substr` |
| `122_Transform_Str.js` | Transforming — `toUpperCase`, `toLowerCase`, `trim`, `repeat`, `padStart`, `padEnd` |
| `123_SC.js` | Conversion — `toString`, `Number`, `parseInt`, `parseFloat` |
| `String_Functions_CheatSheet.md` | Comprehensive cheat sheet covering all string methods with bonus topics (`match`, `matchAll`, `localeCompare`, `normalize`, etc.) |
