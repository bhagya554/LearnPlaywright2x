# Chapter 14: Objects

Creating, accessing, modifying, and working with JavaScript objects — including destructuring, spread, getter/setter methods, and the difference between `let` and `const` with objects.

## Files

| File | Topic |
|------|-------|
| `124_Object.js` | Object basics — empty object, key-value pairs, accessing with dot and bracket notation |
| `125_Object2.js` | Multiple objects, JSON vs JS object syntax, copy by reference with objects |
| `126_Objects_Creation.js` | Comparing objects — two identical objects are not `===` (reference comparison) |
| `127_Objects_Real.js` | Realistic example — config object for browser, timeout, test name |
| `128_Primitive_Ref.js` | Primitive vs reference types — copy by value vs copy by reference |
| `129_Ob_Examples.js` | JSON-style vs JS-style object syntax |
| `130_IQ.js` | Interview question — bracket notation with space in key, `Object.keys`, `Object.values`, `Object.entries` |
| `131_Obj_fn.js` | Methods inside objects — calculator with `add`, `sub`, `mul`, `div` using `this` |
| `132_Object_Destructure.js` | Destructuring — basic, rename, nested, default values, function parameter destructuring |
| `133_Spread.js` | Spread operator — merging objects with `...` |
| `134_Object_GET_SET_Methods.js` | Getters and setters — `get` / `set` with computed full name |
| `135_IQ.js` | Interview questions — `Object.keys`/`values`/`entries`, dynamic properties, `Object.assign`, `Object.freeze`, `Object.seal` |
| `136_Object_Real_Explanation.md` | Real-world example explained — reading `retryCount` from `process.env` with `parseInt` and fallback |
| `136_Object_Real.js` | Real-world config — `ENV` and `EXPECTED_RESPONSE` objects used in test automation |
| `137_Let_Const_Object.js` | `let` vs `const` with objects — can modify properties but cannot reassign `const` |
