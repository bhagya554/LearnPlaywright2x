# Chapter 17: Promises

Understanding JavaScript Promises — creating and consuming promises, resolving callback hell, and using `Promise.all` / `Promise.allSettled` for concurrent operations in test automation contexts.

## Files

| File | Topic |
|------|-------|
| `154_Promise.js` | Promise basics — `new Promise`, `resolve`, `reject`, `.then()` and `.catch()` |
| `155_API_RealExample_Resolve.js` | Real-world API example — resolving with a response object (`status`, `body`) |
| `156_API_RealEx_Reject.js` | Real-world API example — rejecting with an error, explanation of executor function |
| `157_Finally.js` | `.finally()` — cleanup logic that runs regardless of resolve or reject |
| `158_Promise_Resolves_CB_Hell.js` | Using Promises to resolve callback hell — chained `.then()` for browser automation steps |
| `159_Promise_All_AllSettled.js` | `Promise.all()` (fail-fast) vs `Promise.allSettled()` (wait for all) with test automation examples |
| `160_Promise_IQ.js` | Interview questions — chaining, error propagation, `Promise.all` vs `Promise.allSettled` with test scenarios |
