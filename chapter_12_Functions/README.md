# Chapter 12: Functions

Defining and calling functions, the four function types (with/without parameters and return values), function expressions, arrow functions, default/rest parameters, the spread operator, scope, closures, higher-order functions, and pure functions.

## Files

| File | Topic |
|------|-------|
| `96_Functions.js` | Function basics — defining vs calling; overview of the 4 function types |
| `97_Type1_FunctionWithNoretNoArrg.js` | Type 1 — no parameters, no return value; parameter vs argument terminology |
| `98_Type2_FunctionWithParameter_NoRetType.js` | Type 2 — with parameters, no return value |
| `99_Type3_WithoutParameter_ReturnType.js` | Type 3 — no parameters, with `return` |
| `100_Type4_WithParameter_WithReturnType.js` | Type 4 — with parameters and `return` |
| `101_Template_Literal.js` | Template literals inside functions — `` `Hello, ${name}` `` |
| `102_Function_Expression.js` | Function expressions — assigning a function to a `const` |
| `103_Arrow_Function.js` | Arrow functions (ES6) — full form, single-expression shorthand, single-parameter shorthand |
| `104_Arrow_Fn_Real.js` | Declaration vs expression vs arrow function for the same task; arrow functions and the TDZ |
| `105_IIFE.js` | Immediately Invoked Function Expressions (IIFE) — regular and arrow forms |
| `106_Default_Param_Fn.js` | Default parameter values |
| `107_IQ.js` | Interview question — returning a template-literal string from a function |
| `108_Rest_Param_Fn.js` | Rest parameters — collecting arguments into an array with `...` |
| `109_IQ.js` | Interview question — early `return` for status codes; functions without a `return` yield `undefined`; hoisting vs TDZ |
| `110_Spread_IQ.js` | Spread operator — expanding arrays into arguments; `reduce` / `some` |
| `111_Scope_Fn.js` | Global vs local scope; accessing globals from inside a function |
| `112_IQ.js` | Interview question — nested/block scope and where variables are accessible |
| `113_Closure.js` | Closures — inner function retaining access to the outer function's variables |
| `114_Closure.js` | Closure with private state — `makeCounter` returning increment/decrement/get |
| `115_API_Real_Closure.js` | Real-world closure — a retry tracker keeping private attempt count |
| `116_Higher_Order_Fn.js` | Higher-order functions — taking a function as an argument |
| `117_Pure_Fn.js` | Pure vs impure functions — predictable output and side effects |
