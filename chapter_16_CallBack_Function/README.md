# Chapter 16: Callback Functions

Understanding callbacks — synchronous and asynchronous patterns, callback hell, and how callbacks work with parameters and return values, especially in the context of Playwright / JavaScript testing.

## Files

| File | Topic |
|------|-------|
| `143_CallBack.js` | Callback basics — passing a function as an argument, executing it inside another function |
| `144_CB.js` | Another callback example with different function reference style |
| `145_CB_Fn.js` | Named callback functions — defining callbacks separately and reusing them |
| `146_PW_CB.js` | Playwright-style callback — logging `page` and `browser` with a callback |
| `147_JS_CB.js` | JavaScript built-in callbacks — `setTimeout`, `alert`-style simulation inside callbacks |
| `148_Sync_CB.js` | Synchronous callback — callbacks that execute immediately in the same call stack |
| `149_Async_CB.js` | Asynchronous callback — `setTimeout`-based async execution with delayed callback |
| `150_CB_Hell.js` | Callback hell — deeply nested callbacks (pyramid of doom) |
| `151_CB_Hell.js` | Another callback hell example — nested `setTimeout` calls |
| `152_CallBack_WithParameters.js` | Callbacks with parameters — passing data into the callback function |
| `153_CallBack_Return.js` | Callbacks that return values — using the return value of a callback inside the host function |
